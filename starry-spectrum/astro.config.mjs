import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://secamc93.github.io/PortafolioYo/',
  base: '/PortafolioYo/',
  outDir: './dist', // Asegúrate de mantener esta configuración para la carpeta de salida
});

